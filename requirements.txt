# MultiCloud Security Audit Tool — Python dependencies
#
# Phase 1 (core engine + demo): stdlib only, no external dependencies.
# Cloud provider SDKs are installed on demand as you enable each provider.
#
# --------------------------------------------------------------------------
# Phase 2 — AWS provider
# --------------------------------------------------------------------------
boto3>=1.34.0

# --------------------------------------------------------------------------
# Phase 3 — Azure provider
# --------------------------------------------------------------------------
azure-identity>=1.15.0
azure-mgmt-compute>=30.0.0
azure-mgmt-storage>=21.0.0
azure-mgmt-network>=25.0.0
azure-mgmt-resource>=23.0.0
azure-mgmt-authorization>=4.0.0
azure-mgmt-keyvault>=10.0.0
azure-mgmt-monitor>=6.0.0
azure-mgmt-security>=6.0.0
azure-mgmt-sql>=3.0.0
msgraph-sdk>=1.0.0
azure-mgmt-containerservice>=10.0.0
azure-mgmt-containerregistry>=10.0.0
azure-mgmt-cosmosdb>=9.0.0

# --------------------------------------------------------------------------
# Phase 4 — GCP provider
# --------------------------------------------------------------------------
google-auth>=2.0.0
google-api-python-client>=2.100.0
google-auth-httplib2>=0.2.0
google-cloud-storage>=2.0.0

# --------------------------------------------------------------------------
# Optional — development
# --------------------------------------------------------------------------
pyyaml>=6.0
# pytest>=7.0.0
# pytest-asyncio>=0.21.0
